# hakusan-attendance CLAUDE.md

## これは何か
**出欠ドロッパー**（汎用の出欠システム）のソース。どの団体でも使える。LINE・LIFF非依存。
- `attend/` 参加者・主催者向けの静的ページ ／ `api/` Cloudflare Worker と D1
- 主催者の画面は `attend/admin.html`。**合鍵つきのリンク1本**で開く
- `dropper/attendance-hook.js` イベントドロッパーへ貼る連携モジュールの**原本**
- 公開先は **app.dropper-tools.com/attend/**（dropper-app リポジトリから配信）。
  このリポジトリの GitHub Pages はソース置き場で、ルートは案内だけの `index.html`
- API は **api.dropper-tools.com**（この `api/` からデプロイ）

## 要点
- **メニュー方式**：配るURLは団体につき3本（一覧・わたしの回答・集まり具合）。
  `?s={団体ID}` だけで、行事が増えても**貼り直さない**
- **1本目は3画面をタブで束ねてある**（2026-08-24）。**Webで配るならこれ1本でよい**。
  `#my` `#status` を付ければその画面から始まる。LINEを持たない en / in の団体は、これしか道が無い。
  `my.html` / `status.html` は**リッチメニューのように1画面だけ直接ひらきたいとき用**に残してある
  （あちらにタブは出さない。メニューが同じ役目をしているため）
- **参加者の3画面の呼び名は「出欠入力／わたしの回答／出欠確認」にそろえる**（en は
  Answer / My answers / Counts、in は Jawab / Mere jawab / Ginti）。
  **LINEのリッチメニューのボタン名に合わせてある。** あれは L Message 側にあって差し替えが手間で、
  メンバーが毎日見るものなので、こちらを動かす。タブ・見出し・ページタイトル・管理画面の
  「配るURL」・案内文の中でその画面を指す語は、すべてこの3語を使うこと。
  ※ 主催者の管理画面にある節「**出欠の集まり具合**」は別物（名前まで出る主催者用）。こちらは変えない
- **「わたしの回答」「出欠確認」の中身は `attend/attend-views.js` に1か所だけ置く。**
  上の3ページが共有している。**どちらかのHTMLにコピーして書き足さないこと**
  （LINEから来た人とWebから来た人で画面が食い違う）
- **データは開発者のCloudflare（D1）に預かる。** 以前のBYOB（主催者のスプレッドシート）は
  セットアップが重すぎてやめた。経緯は `docs/backend-plan.md`
- **主催者の認証は管理リンク1本**。合鍵は32バイト、サーバーは**SHA-256しか保存しない**。
  受け取りは**必ずPOSTの本文**（クエリだとアクセスログとRefererに残る）。
  管理URLは `admin.html?s=…#k=<合鍵>` で、`#` のうしろはサーバーに送られない。
  **なくすと復旧できない**ので、画面で強く念を押してある
- **識別**：名簿から名前を選ぶ＋端末記憶（localStorageのUUID）。ログイン不要
- **参加者の画面のことばは「団体ごと」に1つ**（`orgs.lang` = ja / en / in）。
  配るURLは3本のまま。参加者は何も選ばない。主催者が団体をつくるときに決め、あとから変えられる
  - 訳は `attend/attend.js` の `DICT` に**1か所だけ**置く。フォルダは増やさない（配るURLが3倍になる）
  - **エラーはサーバーで訳さない。参加者ぶんも主催者ぶんも。** `code` を返し、画面が `err_<code>` で訳す。
    差し込みは `vars`（`{n}` `{name}` `{a}`）。サーバーは `bad(code, ja, vars, extra)` の1本で作る。
    受け取りは `ATTEND.errOf(data)` の**1か所だけ**（管理画面の自前の通信もここを通す）。
    `error` の日本語は、code を知らない古い画面のための保険なので消さないこと
  - **保存される値は「団体のことば」で書く。** 種目が読み取れなかったときの既定
    （`DEFAULT_ITEM`）は参加者に見えるので、主催者の画面のことばで入れると英語の団体に「参加」が残る
  - 最初の1枚は**前回この団体で使った言語**で描く（`localStorage.attend.lang.<団体ID>`）。
    端末の言語で決めないこと（日本の団体に英語の端末で入る人がいる）
  - `fmtDate(ymd, lang)` に**言語を渡し忘れると、英語の画面に日本語の曜日が出る**
- **管理画面のことばは、参加者に見せることばとは別物。** 日本語の主催者が英語の団体を
  運営することがある。管理画面はブラウザのことばを既定にし、画面で切り替えて端末に覚える
  （`localStorage.attend.adminLang`）。`ATTEND.followOrgLang(false)` で団体の設定に従わせない
  - 訳は `attend/admin-i18n.js`。**参加者のページには読ませない**（そのぶん重くなる）
  - 主催者の画面の日付は、**主催者のことば**で書く。サーバーは知らないので `uiLang` を毎回送る
  - ことばの名前（日本語 / English / Hinglish）は、**それぞれの言語のまま**書く
- **回答と紐付けは追記式、集計は latest-row-wins。** `UPDATE` を書きたくなったら立ち止まる
- **未回答者の名前を出してよいのは `tally` と `adminEvents` だけ。**
  参加者に見える `summary` には決して入れない
- **行事とイベントは別物。** 行事＝読み取った要項の置き場。出欠を受け付けるかは
  主催者があとから決める（`taikai.event_id` が空＝未使用）
- **日付は中では `YYYY-MM-DD`、外に出すときだけ `YYYY/MM/DD（曜）`。**
  参加者ページの「あと2日」表示がスラッシュ区切りを前提にしている。ISOのまま返すと黙って出なくなる
- **締切の判定は日本時間で行う。** Workerの時計はUTCなので、そのままだと9時間ずれる
- POSTは `Content-Type: text/plain`（プリフライトが無いぶん1往復速い。新APIは両方受ける）
- localStorageは書いたあと読み直して確かめる（ブロック下では例外を投げず黙って捨てる実装がある）

## ⚠ オリジンを混ぜない
端末IDの記憶は**アドレスの出どころごとに別物**。`app.dropper-tools.com` と
`moviepingpong-art.github.io` の両方に同じ `attend/` があるので、配るURLは
**app.dropper-tools.com に統一する**。混ぜると回答者が名前を選び直すことになる。
`api/src/index.js` の `ALLOW_ORIGINS` を増やすときも同じ理由で慎重に。

## 重要な注意事項
- **デプロイの順番は `git pull` → マイグレーション → `wrangler deploy`。**
  `schema.sql` は `CREATE TABLE IF NOT EXISTS` なので**既存のテーブルには列が増えない**。
  順番を崩すと、新しいコードが存在しない列を読んで本番が落ちる（実際にやった）
- **エラーでもCORSヘッダーを必ず付ける。** 付け忘れると、ブラウザは中身ではなく
  「CORSでブロック」と報告して原因が見えなくなる。**CORSと出たらまず別の失敗を疑うこと**
- `attend.js` / `attend-views.js` / `attend.css` を更新したら、5本のHTMLの `?v=` を**同じ値**に
  書き換える（スマホ・LINE内ブラウザは古いJSを長く抱え込む）。
  **`?v=` は日付ではなく1ずつ増える通し番号。** 実際の日付より先に進んでいるので、
  「今日の日付」を入れると**過去に使った値を再利用**することになり、キャッシュが素通りする
- 消す順番は**出欠 → 行事**。出欠を作ってある行事は消せない
  （逆を許すと出欠だけが親なしで残り、画面から辿れなくなる）
- 「手じまい中」と「締切日をすぎました」は**画面で区別する**。同じ「締切ずみ」に見せると、
  再開できるのかどうか分からない。締切日をすぎたものに再開ボタンは出さない
- 出欠作成に失敗したら**ボタン再押しのみ**（要項を再ドロップしない→カレンダー予定が重複する）

## イベントドロッパーとの連携
- `dropper/attendance-hook.js` が原本。dropper-app の `calendar*/attendance-hook.js` にコピーして使う
- **ドロッパーは何も書き込まない。** カードの「🙋 出欠システムに保存」を押すと、
  読み取り結果を積んだ管理画面（`admin.html#t=…`）を開くだけ。取り込むかどうかは向こうが決める。
  そのため**ドロッパー側に設定が一切ない**
- **取り込みは自動、出欠を作るのは1タップ。** 開催日・締切・種目を見せてから
  「この内容で出欠を受け付ける」。**ここを自動にしない**（読み違えた締切のまま受付が始まると直せない）
- **出欠を作ったら、ドロッパーに知らせて自動で帰す**（`tellDropper` → postMessage と
  `localStorage.dropperAttendDone` の両方）。案内文の「メニューの出欠入力から」は、
  向こうが**これを受け取ったときだけ**出す。送っただけで受付を始めずに閉じると案内文が嘘になるため。
  帰すのはドロッパーから来たときだけ（ブックマークから開いた管理画面では帰さない）
- **管理画面は合鍵を端末に覚えられる**（`localStorage.dropperAdminKey`）。覚えていないと取り込み先が
  分からないので、届いた行事は `dropperPending` に**1時間だけ預かる**（貼り付け直しをさせないため）
- **`#t=` はアドレス欄から消す**（読み直しで二重に取り込まない）。
  記憶から来た合鍵は `#k=` に**書き戻さない**（ドロッパーから開いたタブに合鍵を映さない）
- 開いたままの管理画面に管理リンクを開くと `hashchange` だけで読み直されない。**`reload` させる**
- **ホーム画面に登録したアプリは、ブラウザとは別の入れもの。** 記憶もGoogleログインも
  共有しない。そこから `window.open` すると飛び出して「どの団体か分からない」状態で開くので、
  `attendStandalone_()` が true のときは**同じ画面で移動する**
- **ホーム画面のアプリに管理リンクを持ち込む道は貼り付けだけ**（アドレス欄が無く、
  LINEのリンクを押すと別のブラウザで開く）。管理画面の「すでに団体をお持ちの方」を塞がない。
  この欄は**「団体をつくる」より上に必ず一緒に出す**（`showStart`）。
  つくるほうだけ見せると二つめの団体ができ、配ったURLが古いほうを指したままになる
- **帰り道は来かたで2通り。** 別タブで開かれたなら**閉じる**（`window.opener` があるときだけ）、
  同じ画面で来たなら**前の画面へ戻る**（直前がドロッパーのときだけ。`document.referrer` で見る）
- 符号化は `saveToken`（base64urlのJSON）と `importTaikai` が対。**形を変えるときは両方そろえる**
- **要項のドライブ保存はカレンダー登録と切り離してある**（`attendEnsureYoukou_`）。
- **タブもクリップボードも「押した瞬間」でないと開けない・書けない。**
  要項の保存を待ってから `window.open` / `writeText` を呼ぶと、ポップアップブロックと
  iOS Safari で黙って失敗する。**先に空のタブを開いてから行き先を入れる**。
  貼り付けの逃げ道に落ちたときは **Promise を渡せる `ClipboardItem`** で書く
- **案内文にURLを入れない。** 地図・カレンダー・要項は出欠の回答画面にボタンとして出る。
  長いURLを2本並べると本文より長くなり、肝心の出欠が埋もれる。
  出欠を保存していない回だけ、今までどおりURLを載せる（その回は回答画面が無いため）
- **ただし en/in は出欠のURLだけ載せる。** LINEを使わないのでリッチメニューという
  置き場が無く、書かないと参加者はどこからも出欠に辿り着けない。載せるのは**この1本だけ**で、
  地図もカレンダーも今までどおり落とす。`tellDropper` が `attend-created` と
  `dropperAttendDone` の**両方**で渡す（片方だけだと、ホーム画面のアプリか、
  ふつうのブラウザのどちらかで落ちる）

## 旧GAS版
主催者ごとに表をコピーして Apps Script をデプロイする方式だった。
**2026-08-22 に削除**（`gas/` と `attend.js` の `AKfycb…` 振り分け）。git 履歴に残っている。
`?s=` は団体IDのみ。呼び先は `api.dropper-tools.com` の1本。

## 白山クラブ
- **旧LIFFシステムは一度も運用しないまま廃止**した（2026-08-12）。移行データなし
- 立ち上げ手順は `docs/hakusan-setup.md`
- LINEボットID：OZovWo / @015duxnb（リッチメニューの差し替え先は上記ドキュメント）
- 旧LIFF（2010475547-nDT2DLT8 / -YIPFjUme）と旧GASプロジェクト「大会出欠フォーム」は使わない
